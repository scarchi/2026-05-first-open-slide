name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Fix GitHub Pages asset paths
        run: |
          perl -0pi -e 's#(href|src)="/assets/#$1="/2026-05-first-open-slide/assets/#g' dist/index.html
          perl -0pi -e 's#(["`])/(s|themes|assets)(?=/|["`])#$1/2026-05-first-open-slide/$2#g; s#path:"/"#path:"/2026-05-first-open-slide/"#g; s#to:"/"#to:"/2026-05-first-open-slide/"#g' dist/assets/*.js
          cp dist/index.html dist/404.html
          mkdir -p dist/s/getting-started
          cp dist/index.html dist/s/getting-started/index.html

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
